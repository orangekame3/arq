version: 2

project_name: arq

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/orangekame3/arq/cmd.Version={{.Version}}
    binary: arq

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

release:
  github:
    owner: orangekame3
    name: arq
  prerelease: auto
  draft: false

brews:
  - repository:
      owner: orangekame3
      name: homebrew-tap
    homepage: "https://github.com/orangekame3/arq"
    description: "Local arXiv paper index for fzf-driven exploration"
    license: "MIT"
    test: |
      system "#{bin}/arq", "--help"
